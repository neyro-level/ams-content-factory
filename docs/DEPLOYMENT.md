# Deployment

Production packaging создаётся только в W16. Deploy path: SourceCraft `main` → green verify →
manual confirmed deploy → AMS server smoke checks. До получения infrastructure inputs deployment
имеет статус `BLOCKED_EXTERNAL`.
